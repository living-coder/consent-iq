{
  "name": "consent-iq",
  "version": "1.0.0",
  "description": "Clinical Trial Consent Management System",
  "main": "server.js",
  "scripts": {
    "dev": "flutter run -d chrome",
    "build": "flutter build web --release",
    "serve": "node server.js",
    "start": "npm run build && npm run serve"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "dotenv": "^16.0.3",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
